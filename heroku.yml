# setup:
#   addons:
#     - plan: heroku-mysql
build:
  docker:
    web: Dockerfile
# run:
#   web: /bin/sh -c php-fpm\ run\ -p\ \$PORT
# run:
#   web: docker run -p 80:$PORT -e PORT=$PORT
run:
  web: vendor/bin/heroku-php-nginx