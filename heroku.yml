# setup:
#   addons:
#     - plan: heroku-mysql
build:
  docker:
    web: Dockerfile

# run:
#   web: sed -i -e 's/$PORT/'"$PORT"'/g' /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'

# run:
#   web: docker-php-entrypoint php-fpm -p $PORT
# run:
#   web: php -S 0.0.0.0:$PORT