# setup:
#   addons:
#     - plan: heroku-mysql
build:
  docker:
    web: Dockerfile
# run:
#   web: sed -i -e 's/$PORT/'"$PORT"'/g' /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'
# run:
#   web: docker run -p $PORT -e PORT=$PORT
# run:
#   web: php -p $PORT -e PORT=$PORT